version: '3.9'
services:

  db:
    restart: always
    image: postgres
    container_name: customore #you can change this
    environment:
      - POSTGRES_USER=customore #you can change this
      - POSTGRES_PASSWORD=customore #you can change this
      - POSTGRES_DB=customore #you can change this
      - POSTGRES_PORT=5432
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/

volumes:
  postgres_data: